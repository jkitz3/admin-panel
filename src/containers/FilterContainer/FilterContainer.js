import { FilterSection } from "../../features/adminPanel/components";
// import { useSelector } from "react-redux";
// import { getOrderList } from "../../selectors/orderListSelector";

export const FilterContainer = () => {
  // const orderList = useSelector(getOrderList);
  return <FilterSection></FilterSection>;
};
